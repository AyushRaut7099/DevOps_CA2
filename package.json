{
  "name": "student-feedback-form-tests",
  "version": "1.0.0",
  "description": "Selenium tests for Student Feedback Registration Form",
  "scripts": {
    "test": "mocha test_feedback_form.js --timeout 30000"
  },
  "dependencies": {
    "selenium-webdriver": "^4.18.1"
  },
  "devDependencies": {
    "mocha": "^10.4.0"
  }
}
